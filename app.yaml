name: patio-esperanza
region: sfo

services:
  # -------------------- BACKEND API (FastAPI) --------------------
  - name: api
    http_port: 8000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-1gb
    source_dir: /backend
    github:
      repo: Patio-Esperanza/ProyectoPatioEsperanza
      branch: master
      deploy_on_push: true
    build_command: |
      pip install --no-cache-dir -r requirements.txt
    run_command: |
      PYTHONPATH=. alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
    health_check:
      http_path: /api/health
      initial_delay_seconds: 30
      period_seconds: 15
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 3
    envs:
      - key: DATABASE_URL
        value: "CAMBIAR_POR_URL_DEL_CLUSTER_EXISTENTE"
        scope: RUN_TIME
        type: SECRET
      - key: MIGRATIONS_DATABASE_URL
        value: "CAMBIAR_POR_URL_DEL_CLUSTER_EXISTENTE"
        scope: RUN_TIME
        type: SECRET
      - key: JWT_SECRET
        value: "CAMBIAR_POR_SECRETO_LARGO"
        scope: RUN_TIME
        type: SECRET
      - key: JWT_EXPIRES_MINUTES
        value: "60"
        scope: RUN_TIME
      - key: CORS_ORIGINS
        value: "https://patio-esperanza-xxxxx.ondigitalocean.app"
        scope: RUN_TIME
      - key: SENDGRID_API_KEY
        value: "CAMBIAR_POR_API_KEY"
        scope: RUN_TIME
        type: SECRET
      - key: SENDGRID_FROM_EMAIL
        value: "noreplay@patiolaesperanza.com.mx"
        scope: RUN_TIME

  # -------------------- FRONTEND (Next.js) --------------------
  - name: web
    http_port: 3000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-1gb
    source_dir: /frontend
    github:
      repo: Patio-Esperanza/ProyectoPatioEsperanza
      branch: master
      deploy_on_push: true
    build_command: |
      npm ci
      npm run build
    run_command: |
      npm start
    routes:
      - path: /
    envs:
      - key: NEXT_PUBLIC_API_URL
        value: "${api.PUBLIC_URL}"
        scope: BUILD_TIME
      - key: NEXT_PUBLIC_API_URL
        value: "${api.PUBLIC_URL}"
        scope: RUN_TIME
